#include "../include/philo.h"

int main(int argc, char **argv)
{
	//printf("argc = %d\n", argc);

	if (argc == 5 || argc == 6)
	{
		// input_check();
		// data_init();
		// clean();
	}
	else
	{
		printf("Wrong input!");
	}
	
	return (0);
}